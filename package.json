{
  "name": "json-server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "mkdir public && bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/json-server": "^0.14.8",
    "json-server": "0.17.4"
  }
}