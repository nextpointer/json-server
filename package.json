{
  "name": "json-server",
  "scripts": {
    "start": "mkdir public && json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "json-server": "0.17.4"
  }
}